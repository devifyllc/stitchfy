# Legacy Modernization Capability

Status: **skeleton only** (Phase 0). See `docs/architecture/ROADMAP.md` Phase 8.

Note: `modernization` is an addition to the base Solution Blueprint field
list (project/business/.../qa) — see the "Adaptation" note in
`docs/architecture/ARCHITECTURE.md` for why it gets its own top-level field
rather than being folded into `systems`.

## Planned structure

```
modernization/
├── discovery/
├── dependency-analysis/
├── migration/
├── modernization-strategy/
└── schemas/   ← already present
```
