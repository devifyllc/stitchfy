# Integrations Capability

Status: **skeleton only** (Phase 0). See `docs/architecture/ROADMAP.md` Phase 4.

## Planned structure

```
integrations/
├── api/         ← REST API integration generators
├── webhooks/    ← webhook subscription/dispatch generators
├── saas/        ← SaaS-specific connectors
├── validators/
└── schemas/     ← already present
```
