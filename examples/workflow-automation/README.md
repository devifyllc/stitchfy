# Workflow Automation Examples

Placeholder — no worked example yet. The existing top-level `examples/*.md`
files (beauty-salon.md, massage-spa.md, medical-center.md) remain the
website pipeline's examples; this directory is reserved for a
workflow-automation-focused business spec once
`framework/capabilities/workflow-automation` is implemented (Phase 3/9).
