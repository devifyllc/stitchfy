# Integration Templates

`framework/capabilities/integrations` is now a fully implemented capability (see its own `README.md`), so this is no longer a placeholder for a future `execute()`.

For a real starter Markdown template, see `templates/solution/api-platform.md` (the full `## Integrations` sub-field set). For completed worked examples, see `examples/solution/rest-export-ready.md` and `examples/reference/order-platform.md`.
