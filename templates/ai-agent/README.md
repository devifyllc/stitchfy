# AI Agent Templates

`framework/capabilities/ai-agents` is now a fully implemented capability
(see its own `README.md`) — this is no longer a placeholder for a future
`execute()`.

For a real starter Markdown template, see
`templates/solution/ai-assisted-workflow.md` (the single real
`## AI Agent Needs` heading). For completed worked examples, see
`examples/solution/customer-support-agent.md` and
`examples/reference/appointment-automation-ai.md`.
