# Modernization Templates

`framework/capabilities/modernization` is now a fully implemented capability (see its own `README.md`), including optional codebase evidence (Phase 8.5A) and the `generic-java-replatform` export adapter (Phase 8.5B). It's no longer a placeholder for a future `execute()`.

For a real starter Markdown template, see `templates/solution/legacy-modernization.md`. For a completed worked example covering the full chain from requirements through the exporter, see `examples/reference/legacy-java-modernization.md` and `docs/reference/LEGACY_JAVA_MODERNIZATION.md`.
