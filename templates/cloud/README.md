# Cloud Templates

Placeholder. Will hold deployment/infrastructure templates once
`framework/capabilities/cloud` has a real `execute()` and at least one
`framework/providers/cloud/*` adapter exists — see
`docs/architecture/ROADMAP.md` Phase 7.
